# --- supplied dataset: must never be committed -----------------------------
# Only the dataset itself. The generated outputs under outputs/ ARE a required
# deliverable, and they carry masked text only -- tests/test_no_leakage.py
# asserts no secret survives into any of them.
data/

# --- python ----------------------------------------------------------------
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# --- tooling ---------------------------------------------------------------
.vercel/
.DS_Store
.env
.env.local
.vercel

# --- personal recording aid, not part of the submission --------------------
tools/
